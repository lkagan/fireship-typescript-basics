let font;
font = 'bold';
const person2 = {
    first: 'Usain',
    last: 'Bold',
    fast: true,
};
// Functions
function pow(x, y) {
    return Math.pow(x, y).toString();
}
// Arrays
const arr = [];
arr.push(1);
// arr.push('12');  // Error
// arr.push(false); // Error
// Array of objects
const people = [];
people.push({
    first: 'Larry',
    last: 'Kagan'
});
const myArr = [1, '12', false];
const myOptionalArr = [];
